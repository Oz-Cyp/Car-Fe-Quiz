const cars = {
    // getCarById: () => `"/cars/${carId}"`,
    getCars: () => `/cars`,
  };
const customers ={
  getCustomers: () => `/customers`
}
  const sales = {
    getSales: () => `/sales`,
}

  export {
  cars,
  customers,
  sales
  };

  